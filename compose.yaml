name: pet

services:
  inference-smoke:
    image: ${INFERENCE_IMAGE:-pet-inference}:${IMAGE_TAG:-dev}
    build:
      context: .
      dockerfile: docker/Dockerfile.inference
    command: ["--device", "cpu"]
    read_only: true
    tmpfs:
      - /tmp

  trainer-smoke:
    platform: linux/amd64
    image: ${TRAINER_IMAGE:-pet-trainer}:${IMAGE_TAG:-dev}
    build:
      context: .
      dockerfile: docker/Dockerfile.trainer
    command: ["python", "-m", "pet.cli.container_smoke", "--device", "cuda"]
    gpus: all
    read_only: true
    tmpfs:
      - /tmp
